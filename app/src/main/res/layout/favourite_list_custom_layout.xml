<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">
    <RelativeLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:padding="10dp"
        android:layout_marginTop="5dp"
        android:id="@+id/favouriteListRootLayout"
        android:background="#f9ee9f"
        android:layout_height="70dp">
        <ImageView
            android:id="@+id/favouriteListLogo"
            android:src="@drawable/ic_favourite"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:layout_width="40dp"
            android:layout_height="40dp"/>
        <TextView
            android:id="@+id/favouriteListTitleTextView"
            android:text="Title"
            android:layout_toRightOf="@+id/favouriteListLogo"
            android:layout_toLeftOf="@+id/favouriteListDeleteImageView"
            android:gravity="center"
            android:layout_marginRight="5dp"
            android:textSize="18sp"
            android:textColor="#9C27B0"
            android:layout_centerInParent="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <ImageView
            android:id="@+id/favouriteListDeleteImageView"
            android:layout_alignParentEnd="true"
            android:layout_centerInParent="true"
            android:src="@drawable/ic_delete"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </RelativeLayout>
</layout>